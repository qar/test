{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "gen:migrate": "drizzle-kit generate:sqlite --out migrations --schema src/schema/migration.ts",
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "drizzle-orm": "^0.30.6",
    "hono": "^4.1.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "drizzle-kit": "^0.20.14",
    "wrangler": "^3.32.0"
  }
}
